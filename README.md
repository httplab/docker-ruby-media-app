# Docker image with ffmpeg and ruby

Based on [httplab/docker-ruby](httplab/docker-ruby). 
In addition contains ffmpeg built with with non-free fdk-aac audio codec, x264, x265 video codecs and their dependencies.